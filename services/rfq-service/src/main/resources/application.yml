server:
  port: 8083

spring:
  application:
    name: rfq-service
  datasource:
    url: jdbc:postgresql://localhost:5432/rfq_db
    username: admin
    password: ${DB_PASSWORD:supersecretpassword}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  rabbitmq:
    host: localhost
    port: 5672
    username: ${RABBITMQ_USER:guest}
    password: ${RABBITMQ_PASS:guest}
