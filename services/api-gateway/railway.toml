[build]
builder = "DOCKERFILE"
dockerfilePath = "services/api-gateway/Dockerfile.railway"

[deploy]
startCommand = "java -jar app.jar"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
