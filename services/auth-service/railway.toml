[build]
builder = "DOCKERFILE"
dockerfilePath = "services/auth-service/Dockerfile.railway"

[deploy]
startCommand = "java -jar app.jar"
healthcheckPath = "/api/auth/login"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
